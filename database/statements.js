const statements = {
    checkRecentVisit: "SELECT * FROM view WHERE visitor_ip = ? AND article_id = ? AND timestamp >= NOW() - INTERVAL 30 MINUTE",
    recordVisit: "INSERT INTO view (route_name, visitor_ip, article_id) VALUES (?, ?, ?)",
    viewsByArticle: "SELECT COUNT(*) AS views FROM view WHERE article_id = ?",
    articleById: "SELECT article.title, article.subtitle, article.content, DATE_FORMAT(article.created_at, '%d/%m/%Y %H:%i') AS formatted_created_at, media.src AS media_src, media.external_ref AS media_external_ref, category.name AS category_name FROM article JOIN media ON article.media_id = media.id JOIN category ON article.category_id = category.id WHERE article.id = ?",
    articles: "SELECT article.id, article.title, article.subtitle, DATE_FORMAT(article.created_at, '%d/%m/%Y %H:%i') AS formatted_created_at, media.src AS image_src, category.name AS category_name FROM article JOIN media ON article.media_id = media.id JOIN category ON article.category_id = category.id ORDER BY article.created_at DESC, article.id DESC",
    latestArticles: "SELECT article.id, article.title, article.subtitle, DATE_FORMAT(article.created_at, '%d/%m/%Y %H:%i') AS formatted_created_at, media.src AS image_src, category.name AS category_name FROM article JOIN media ON article.media_id = media.id JOIN category ON article.category_id = category.id ORDER BY article.created_at DESC, article.id DESC LIMIT 4",
    popularArticles: "SELECT article.id, article.title, article.subtitle, COUNT(view.id) AS views, media.src AS image_src, category.name AS category_name FROM article JOIN media ON article.media_id = media.id JOIN category ON article.category_id = category.id LEFT JOIN view ON article.id = view.article_id GROUP BY article.id ORDER BY views DESC, article.created_at DESC, article.id DESC LIMIT 4",
    relatedArticles: "SELECT article.id, article.title, article.subtitle, DATE_FORMAT(article.created_at, '%d/%m/%Y %H:%i') AS formatted_created_at, media.src AS image_src, category.name AS category_name FROM article JOIN media ON article.media_id = media.id JOIN category ON article.category_id = category.id WHERE category.name = ? AND article.id != ? ORDER BY article.created_at DESC, article.id DESC LIMIT 3",
    carouselArticles: "SELECT article.id, article.title, article.subtitle, DATE_FORMAT(article.created_at, '%d/%m/%Y %H:%i') AS formatted_created_at, media.src AS image_src, category.name AS category_name FROM article JOIN media ON article.media_id = media.id JOIN category ON article.category_id = category.id WHERE media.src LIKE '%carousel%' ORDER BY article.created_at DESC, article.id DESC LIMIT 2",
    esportsFeatureArticles: "SELECT article.id, article.title, article.subtitle, DATE_FORMAT(article.created_at, '%d/%m/%Y %H:%i') AS formatted_created_at, media.src AS image_src, category.name AS category_name FROM article JOIN media ON article.media_id = media.id JOIN category ON article.category_id = category.id WHERE media.src LIKE '%esports_feature%' ORDER BY article.created_at DESC, article.id DESC LIMIT 2",
    playstationFeatureArticle: "SELECT article.id, article.title, article.subtitle, DATE_FORMAT(article.created_at, '%d/%m/%Y %H:%i') AS formatted_created_at, media.src AS image_src, category.name AS category_name FROM article JOIN media ON article.media_id = media.id JOIN category ON article.category_id = category.id WHERE media.src LIKE '%playstation_feature%' ORDER BY article.created_at, article.id DESC LIMIT 1",
    playstationListArticles: "SELECT article.id, article.title, article.subtitle, DATE_FORMAT(article.created_at, '%d/%m/%Y %H:%i') AS formatted_created_at, media.src AS image_src, category.name AS category_name FROM article JOIN media ON article.media_id = media.id JOIN category ON article.category_id = category.id WHERE media.src LIKE '%playstation-list%' ORDER BY article.created_at DESC, article.id DESC LIMIT 2",
    nintendoFeatureArticle: "SELECT article.id, article.title, article.subtitle, DATE_FORMAT(article.created_at, '%d/%m/%Y %H:%i') AS formatted_created_at, media.src AS image_src, category.name AS category_name FROM article JOIN media ON article.media_id = media.id JOIN category ON article.category_id = category.id WHERE media.src LIKE '%nintendo_feature%' ORDER BY article.created_at DESC, article.id DESC LIMIT 1",
    nintendoListArticles: "SELECT article.id, article.title, article.subtitle, DATE_FORMAT(article.created_at, '%d/%m/%Y %H:%i') AS formatted_created_at, media.src AS image_src, category.name AS category_name FROM article JOIN media ON article.media_id = media.id JOIN category ON article.category_id = category.id WHERE media.src LIKE '%nintendo-list%' ORDER BY article.created_at DESC, article.id DESC LIMIT 2",
    featureArticleByCategory: "SELECT article.id, article.title, article.subtitle, DATE_FORMAT(article.created_at, '%d/%m/%Y %H:%i') AS formatted_created_at, media.src AS image_src, category.name AS category_name FROM article JOIN media ON article.media_id = media.id JOIN category ON article.category_id = category.id WHERE category.name = ? ORDER BY article.created_at DESC, article.id DESC LIMIT 1",
    articlesByCategory: "SELECT article.id, article.title, article.subtitle, DATE_FORMAT(article.created_at, '%d/%m/%Y %H:%i') AS formatted_created_at, media.src AS image_src, category.name AS category_name FROM article JOIN media ON article.media_id = media.id JOIN category ON article.category_id = category.id WHERE category.name = ? ORDER BY article.created_at DESC, article.id DESC LIMIT 4 OFFSET 1",
    logo: "SELECT src AS image_src FROM media WHERE src LIKE '%logo%' ORDER BY id DESC LIMIT 1",
    headerAd: "SELECT src AS image_src, external_ref FROM media WHERE src LIKE '%header_ad%' ORDER BY id DESC LIMIT 1",
    singleAd: "SELECT src AS image_src, external_ref FROM media WHERE src LIKE '%single-ad%' ORDER BY id DESC LIMIT 1",
    asideAds: "SELECT src AS image_src, external_ref FROM media WHERE src LIKE '%aside-ad%' ORDER BY id DESC LIMIT 2",
    videoPreviews: "SELECT article.id, article.title, DATE_FORMAT(article.created_at, '%d/%m/%Y %H:%i') AS formatted_created_at, media.src AS image_src FROM article JOIN media ON article.media_id = media.id WHERE media.src LIKE '%video-thumbnail%' ORDER BY article.created_at DESC, article.id DESC LIMIT 6",
    categories: "SELECT * FROM category",
    editor: "SELECT author.first_name, author.last_name, specialty.name AS specialty_name, author.introduction, media.src AS image_src FROM author JOIN specialty ON author.specialty_id = specialty.id JOIN media ON author.media_id = media.id WHERE media.src LIKE '%author%' ORDER BY author.id DESC LIMIT 1",
    authorByArticleId: "SELECT author.first_name, author.last_name, specialty.name AS specialty_name, author.introduction, media.src AS image_src FROM author JOIN specialty ON author.specialty_id = specialty.id JOIN media ON author.media_id = media.id JOIN article ON author.id = article.author_id WHERE article.id = ?",
    authorLogin: "SELECT username, password FROM author WHERE username = ?",
    authorProfile: "SELECT username, first_name, last_name, role, specialty_id, introduction, media_id FROM author WHERE username = ?"
}

module.exports = {statements};